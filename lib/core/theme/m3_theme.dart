import 'package:flutter/material.dart';

final m3ColorScheme =
    ColorScheme.fromSeed(seedColor: const Color.fromRGBO(0, 98, 162, 1));

final m3Theme = ThemeData.light().copyWith(
  colorScheme: m3ColorScheme,
);
