import 'package:flutter/material.dart';

class CircleMapPage extends StatelessWidget {
  const CircleMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('マップ'),
      ),
    );
  }
}
