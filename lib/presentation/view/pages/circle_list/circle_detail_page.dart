import 'package:flutter/material.dart';

class CircleDetailPage extends StatelessWidget {
  const CircleDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('サークル詳細'),
      ),
    );
  }
}
